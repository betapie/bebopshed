
% #(set! paper-alist
%   (cons '("lick_paper_size" . (cons (* 540 pt) (* 100 pt))) paper-alist))

% \paper {
%   #(set-paper-size "lick_paper_size")
%   indent = 0
%   % line-width = 200
% }

\header {
    tagline = "Created by Bebopshed"
}